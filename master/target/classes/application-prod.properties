# Production / integration profile
# Enable Redis-backed Spring Session explicitly for multi-node deployment.
spring.session.store-type=${SESSION_STORE_TYPE:redis}
spring.devtools.restart.enabled=false
